# 多层内网渗透辅助工具 · 产品设计文档（PRD）

> 版本：v1.0（开发落地版）
> 日期：2026-09-08
> 状态：已确认需求，待进入技术设计
> 文档类型：产品需求文档（含架构建议、数据模型、分期规划）

---

## 0. 文档说明

本文档面向开发落地，需求来源于两部分：

1. **竞品与场景调研**：CTF 多层内网渗透题目的公开 Writeup（春秋云镜、HTB、THM、国内赛事内网题）与现有工具生态（蚁剑/冰蝎/哥斯拉、frp/nps、Neo-reGeorg、EW/Stowaway/Venom、chisel/ligolo-ng、TscanPlus、TopoScan 等）；
2. **需求方两轮决策确认**：覆盖场景定位、技术栈、功能边界、自动化程度、MVP 范围、开源计划等 24 项关键决策。

文中所有需求条目均标注优先级（P0 = 一期必须，P1 = 一期应尽量，P2 = 二期规划）。

---

## 1. 项目概述

### 1.1 一句话定位

一款面向限时 CTF / 靶场场景的**本地 Web 面板工具**，以 **Shell 为中心**，把「Shell 注入与获取 → 代理创建与转发 → 多层资产管理」三件事打通成一条一体化工作流。

### 1.2 背景与问题

CTF 多层内网渗透题的典型链路是：

```
Web 漏洞（RCE/上传/SQL 写马）
   → 获得入口机 Webshell
   → 信息收集 / 出网探测
   → 建立第一层代理（HTTP 隧道或反向 Socks）
   → 穿透 DMZ → 第二层内网 → 第三层（常有域）
   → 逐层拿 Shell、逐层架代理、逐层收集凭据与 Flag
```

选手在这条链路中的真实痛点：

| 痛点 | 现状 |
|---|---|
| 工具碎片化 | 蚁剑管马、frp/EW 管代理、Excel/脑子记资产，三者互不感知 |
| 限时压力 | 比赛限时，大量时间消耗在"上传哪个代理二进制、写什么配置、怎么串链"的重复劳动上 |
| 多层拓扑失控 | 三层以上内网时，选手靠记忆维护"哪个 shell 对应哪台机、代理从哪台跳到哪台"，出错率极高 |
| 凭据散落 | 各层收集的账号/密码/哈希记在笔记软件里，横向移动时无法快速匹配复用 |
| 赛后复盘困难 | 没有操作时间线，Writeup 全靠赛后回忆补 |

### 1.3 产品机会

现有工具中，**没有一款把"Shell 管理、代理编排、多层资产管理"围绕 CTF 限时场景打通**：

- 蚁剑/冰蝎/哥斯拉：只管单层的马，不管代理链路和资产拓扑；
- frp/EW/Venom：只管代理本身，不感知"这个代理是为哪台资产、哪条攻击路径服务的"；
- TscanPlus/TopoScan：贴近红队实战，但重、且非为 CTF 限时节奏设计。

**本产品的差异化：以"跳板链"为一等公民，每一个 Shell、每一条代理、每一项资产都挂在拓扑链上，所见即所得。**

### 1.4 项目命名候选（开源项目，待需求方选定）

| 候选名 | 寓意 |
|---|---|
| **PivotHub** | Pivot（跳板）+ Hub（中枢），直接点题 |
| **ShellBridge** | 以 Shell 为桥，贯穿多层内网 |
| **ChainShell** | 强调"链"式跳板结构 |
| **DeepPivot** | 深层跳板，呼应多层内网 |
| **LianTou（链透）** | 中文名备选，链路穿透 |

> 建议：英文主名 + 中文副名，如 **PivotHub（链透中枢）**。

---

## 2. 目标用户与使用场景

### 2.1 目标用户

**有一定经验的 CTF 选手**（核心画像）：
- 熟悉蚁剑/frp 等基础工具，但讨厌重复的手工编排；
- 打限时赛（2h–48h）与限时靶场（HTB/THM 单机限时、春秋云镜场景赛）；
- 诉求：**要效率、要快捷、要少切窗口、要不出错**。

次要用户：打多层靶场练习的学习者（可使用"自动档"降低门槛）。

### 2.2 核心使用场景

**场景 A：限时赛三层内网（旗舰场景）**
选手拿到入口 Webshell → 面板登记资产 → 一键探测出网方式 → 推荐并半自动拉起第一层反向 Socks → 通过代理扫描第二层 → 拿到第二层 shell 后在拓扑图上"从节点 A 挂到节点 B" → 继续纵深 → 全程凭据、Flag、操作自动入时间线 → 赛后一键导出 Markdown Writeup 初稿。

**场景 B：日常靶场练习**
不限时，选手用"自动档"跑通全流程，观察工具每一步做了什么，作为学习路径。

**场景 C：赛后复盘 / Writeup 输出**
基于操作时间线 + 拓扑图 + 截图，导出 Markdown / HTML 交互报告。

### 2.3 运行环境假设

- **攻击端**：单机本地使用（Kali Linux 或选手个人电脑），面板仅监听 `127.0.0.1`，无需认证；
- **靶机端**：Linux + Windows 均覆盖（含域渗透场景）；
- **部署形态**：Python 单命令启动（`pip install` 或 release 打包），本地 SQLite 持久化。

---

## 3. 产品总体架构

### 3.1 技术选型（已确认）

| 层 | 选型 | 说明 |
|---|---|---|
| 语言 | **Python 3.10+** | 开发快、生态丰富（需求方指定） |
| 后端 | FastAPI + WebSocket | REST 管资产/配置，WebSocket 推实时事件（代理回连、终端输出、时间线流） |
| 前端 | Vue 3 / React + 拓扑图库（ECharts Graph 或 vis-network） | 拓扑可视化为核心界面 |
| 数据库 | SQLite（SQLAlchemy ORM） | 本地单机，零依赖 |
| 代理调度 | 子进程编排 + 适配器模式 | 每个代理工具一个 Adapter，统一管理生命周期 |
| 终端交互 | 基于已有 Shell 会话的虚拟终端（WebSocket 双向） | 不追求完整 PTY，追求"够快" |

### 3.2 架构分层

```
┌─────────────────────────────────────────────┐
│  Web 面板（浏览器，127.0.0.1:PORT）          │
│  ├─ 拓扑画布   ├─ 资产/凭据/Flag 表格        │
│  ├─ 代理编排台 ├─ 虚拟终端                   │
│  └─ 时间线/笔记/看板/导出                    │
├─────────────────────────────────────────────┤
│  核心服务层（FastAPI）                       │
│  ├─ Shell 管理器（一句话马/自定义马协议）     │
│  ├─ 代理编排器（Adapter 调度 + 出网探测）     │
│  ├─ 资产引擎（主机/网段/跳板链/凭据/Flag）    │
│  ├─ 时间线引擎（自动事件 + 手动笔记）         │
│  └─ 报告生成器（MD / HTML / JSON）           │
├─────────────────────────────────────────────┤
│  适配器层（可插拔）                          │
│  ├─ frp / nps  ├─ Neo-reGeorg              │
│  ├─ EW / Stowaway / Venom                   │
│  └─ chisel / ligolo-ng                      │
├─────────────────────────────────────────────┤
│  持久层：SQLite（资产/凭据/拓扑/时间线/配置） │
└─────────────────────────────────────────────┘
```

### 3.3 核心数据模型（一等公民：**跳板链**）

```
Project(一次比赛/一个靶场)
 ├─ Host(资产主机)
 │    ├─ ip / hostname / os / 网段层级(L1/L2/L3...)
 │    ├─ 权限(www-data/root/SYSTEM...) / 备注
 │    ├─ Shell 会话[1..n]（类型/连接信息/存活状态）
 │    ├─ Credential[0..n]（账号/密码/哈希/来源主机/复用标注）
 │    └─ Flag[0..n]（内容/阶段/提交状态）
 ├─ ProxyLink(代理链路)
 │    ├─ 类型(frp/Neo-reGeorg/EW/...) / 方向(正向/反向)
 │    ├─ 入口节点(Host) → 出口节点(Host)  ← 构成链
 │    ├─ 本地监听(统一 Socks 入口端口映射)
 │    └─ 运行状态/进程句柄/配置快照
 ├─ Timeline(操作时间线：自动事件+手动笔记)
 └─ Report(导出的报告快照)
```

> 设计要点：`ProxyLink` 必须显式建模"入口 Host → 出口 Host"，拓扑图 = Host 节点 + Shell 标记 + ProxyLink 边的有向图。这是本产品区别于所有现有工具的数据根基。

---

## 4. 功能需求详述

### M1 · Shell 注入与获取（P0）

> 一期范围：**自研一句话马（菜刀/蚁剑格式）+ 自定义加密马 + 终端固化（M1-8）**；冰蝎/哥斯拉协议兼容列为二期。
>
> ⚠ 补充说明（需求方追加）：实际拿到的大多是**基于 Web 应用 RCE 落地的 WebShell**，属于「一次请求一次执行」的伪终端，**不是可交互终端**。因此「把伪终端固化为稳定交互 TTY」不是可选增强，而是渗透流程中的**必经环节**——它直接决定后续能否正常执行 `su`、`ssh`、`sudo`、`vim`、`top`、`mysql` 等交互式程序，以及能否安全使用 `Ctrl+C`、方向键、Tab 补全。故将其提为 **P0**（M1-8）。

| 编号 | 需求 | 优先级 |
|---|---|---|
| M1-1 | Webshell 生成器：一键生成 PHP/JSP/ASP/ASPX 一句话马与自定义马，支持混淆模板（字符串拼接/assert 变体/免杀参数化） | P0 |
| M1-2 | Shell 连接管理：登记 URL/密码/编码器，连通性测试，存活心跳，断线标记 | P0 |
| M1-3 | 虚拟终端：选中 Shell 后获得类终端体验（命令执行、回显、历史、Tab 补全基础命令） | P0 |
| M1-4 | 文件管理：浏览/上传/下载/编辑（上传代理二进制依赖此能力） | P0 |
| M1-5 | 自定义马协议：客户端-服务端加密通信（AES 密钥可配），自带基础信息收集回传（whoami/uname/ip/网卡列表，自动入库资产表） | P1 |
| M1-6 | Shell 注入辅助：常见写马姿势速查（SQL 写马、日志写马、文件包含、模板注入写马），生成对应 payload 并一键尝试 | P1 |
| M1-7 | 冰蝎/哥斯拉协议栈兼容（复现其客户端协议） | **P2（二期）** |
| M1-8 | **终端固化（TTY Upgrade，P0 关键能力）**：WebShell 本质是「一次性请求-响应」的伪终端，无 TTY、无 job control，`su`/`ssh`/`vim`/`top` 等交互程序不可用，`Ctrl+C` 还会误杀马本身。面板需把「伪终端 → 稳定交互终端」这一步做成**可检测、可一键执行、可验证**的标准化流程：<br>· **交互能力检测**：自动执行 `tty` / `echo $TERM` / `stty size` / 工具探测，输出「是否有 TTY / TERM 类型 / 可用固化工具」结论；<br>· **固化技法库（插件化）**：Linux（Python PTY、`script -qc`、socat 全交互、nc+FIFO、`bash -i`、环境变量修正、`Ctrl+Z → stty raw` 收尾、`reset`）与 Windows（PowerShell 交互会话、ConPTY、cmd 加固、WinRM 稳定会话），每条标注依赖工具、可靠性、风险等级；<br>· **一键执行与验证**：技法可直接发送到虚拟终端执行，面板根据回显判定是否真正拿到 PTY，成功后把会话标记为「已固化 TTY」并写入时间线；<br>· **终端形态可视化**：提示符随形态变化（`$` → `user@host:path$` → `[user@host] ~ #`），Shell 列表与拓扑节点同步展示固化状态 | P0 |

### M2 · 代理创建与转发（P0，差异化核心）

**实现路线（已确认）：集成调度现有工具，自研编排层，不重新造代理协议。**

| 编号 | 需求 | 优先级 |
|---|---|---|
| M2-1 | 适配器集成：frp、nps、Neo-reGeorg、EW、Stowaway、Venom、chisel、ligolo-ng 八款工具，每款一个 Adapter，统一抽象为「生成配置 → 部署 → 拉起 → 监听回连 → 登记链路 → 健康检查 → 销毁」生命周期 | P0 |
| M2-2 | **出网探测**：通过已有 Shell 自动执行 ICMP/DNS/HTTP/TCP 出站探测（内置探测服务器模式或选手指定 VPS），输出"正向/反向/仅 HTTP/仅 DNS"结论并**推荐隧道方案**（如：可反向 → frp；仅 HTTP → Neo-reGeorg；仅 DNS → iodine 提示） | P0 |
| M2-3 | **两档自动化**：<br>· 半自动档（默认）：工具生成完整命令与配置，选手复制到 Shell 执行，面板监听回连并自动登记拓扑；<br>· 自动档（可开关）：经已有 Webshell 自动上传代理二进制（自动识别 OS/架构选包）→ 执行 → 回连 → 登记，全程一键 | P0 |
| M2-4 | 多级串联编排：在拓扑图上选择"从节点 B 再向内打一层"，自动计算链路参数（上一层代理的本地端口即下一层的网络入口），生成串联配置 | P0 |
| M2-5 | **统一 Socks 入口**：所有存活链路汇总为一张"本地端口 → 目标网段"映射表，每条链路提供本机 Socks5 入口 | P0 |
| M2-6 | 工具联动：<br>· 一键生成 proxychains 配置（含多级链）；<br>· 生成 msfconsole `route add` / autoroute 配置片段；<br>· 内嵌简易工具：端口扫描（经代理）、常见服务爆破、SSH/RDP 快速连接入口 | M2-6 前两项 P0，内嵌工具 P1 |
| M2-7 | 隧道类型覆盖：Socks5 正/反向、HTTP 隧道、TCP/UDP 端口转发（lcx 式）、特殊隧道（ICMP/DNS，推荐+编排外部工具即可） | 前三项 P0，特殊隧道 P1 |
| M2-8 | 代理健康看板：每条链路延迟/存活/流量计数，断链告警并支持一键重拉 | P1 |

### M3 · 多层资产管理（P0）

| 编号 | 需求 | 优先级 |
|---|---|---|
| M3-1 | 主机清单：IP/主机名/OS/开放服务/当前权限/所在层级/备注，支持手动登记与 Shell 回传自动入库 | P0 |
| M3-2 | **网络拓扑图（核心界面）**：分层展示跳板关系的有向图——节点=主机（标注权限、Shell 状态、Flag 数），边=代理链路（标注工具/方向/状态）；支持拖拽布局、按层级自动排布、点击节点弹出操作面板（开终端/起代理/登记凭据） | P0 |
| M3-3 | 凭据库：账号/密码/哈希/票据登记，标注来源主机与适用服务；"凭据复用推荐"——选中目标主机时自动列出同网段/同域可能可复用的凭据 | P0 |
| M3-4 | 攻击路径/操作时间线：所有自动事件（拿到 Shell、拉起代理、发现主机、收集凭据、拿到 Flag）+ 手动笔记，按时间轴呈现，可关联到具体主机节点 | P0 |
| M3-5 | 网段管理：登记发现的网段（10.x/172.x/192.168.x），拓扑按网段分区着色 | P1 |

### M4 · CTF 特色功能（P1）

| 编号 | 需求 | 优先级 |
|---|---|---|
| M4-1 | **Flag 收集墙**：按主机/阶段记录已拿 Flag，支持一键复制提交，看板展示完成度 | P1 |
| M4-2 | 内嵌 Markdown 笔记：可关联到主机/事件，随资产沉淀 | P1 |
| M4-3 | 比赛计时/倒计时与阶段看板：设定比赛时长，看板显示"已控主机数/层级深度/Flag 进度/剩余时间" | P1 |
| M4-4 | **Writeup 半自动生成**：以时间线为骨架 + 拓扑图快照 + 笔记，生成 Markdown 初稿（含占位截图位），导出后可人工润色 | P1 |

### M5 · 提权命令速查（轻量版，P1）

| 编号 | 需求 | 优先级 |
|---|---|---|
| M5-1 | 红队命令库：参考 TscanPlus 模式，按场景（Linux 提权/Windows 提权/域渗透/信息收集/权限维持/横向移动）分类的命令模板库，支持变量替换（如 {IP} 自动填当前主机）、一键复制到虚拟终端 | P1 |
| M5-2 | 智能建议：根据当前主机 OS/权限/内核版本，置顶推荐匹配的提权路径（如内核版本命中已知 exp 则提示） | P2 |

### M6 · 数据持久化与复盘导出（P0）

| 编号 | 需求 | 优先级 |
|---|---|---|
| M6-1 | 本地 SQLite 持久化：项目维度保存全部资产/凭据/拓扑/时间线/笔记，支持多项目切换 | P0 |
| M6-2 | 导出格式：Markdown（Writeup 初稿）、HTML 交互报告（内嵌拓扑图）、JSON 原始数据 | P0 |
| M6-3 | 项目导入/导出打包（含截图等附件），便于换机或分享复盘 | P1 |

---

## 5. 非功能需求

| 类别 | 要求 |
|---|---|
| 安全 | 面板仅监听 127.0.0.1；代理二进制传输在受控实验环境内进行，文档中明确"仅限授权靶场使用"声明；项目开源需附带合规免责声明 |
| 性能 | 单机场景：同时管理 ≥ 20 个 Shell 会话、≥ 10 条代理链路不卡顿；拓扑图 ≥ 100 节点可流畅渲染 |
| 可靠性 | 代理子进程崩溃自动检测并标记；工具自身重启后可从 SQLite 恢复项目状态（代理进程需重拉，状态自动核对） |
| 兼容性 | 攻击端：Kali Linux / macOS / Windows（WSL）；靶机端：Linux + Windows 双覆盖 |
| 可扩展性 | 代理 Adapter、命令库、马的模板均为插件化设计（目录约定 + 元数据描述文件），社区可贡献 |
| 合规 | 文档与 README 显著位置标注：仅用于 CTF/授权靶场/教学，禁止未授权使用 |

---

## 6. 关键交互流程（旗舰链路）

```
1. 选手通过 Web 漏洞拿到入口马
   → 面板【Shell 管理】添加连接 → 连通性测试通过
   → 自动回传基础信息 → 【资产】自动创建 Host-L1 节点

2. 选中入口会话 → 点击【交互能力检测】
   → 自动执行 tty / echo $TERM / stty size / 工具探测
   → 结论：无 TTY（tty = not a tty）、TERM=dumb、可用 python3 / script / socat
   → 面板推荐固化技法：Python PTY（备选 script -qc）

3. 点击【执行并固化】→ 面板发送 `python3 -c 'import pty; pty.spawn("/bin/bash")'`
   → 回显出现交互提示符 → 判定拿到 PTY → 会话标记「已固化 TTY」
   → 再做收尾：Ctrl+Z → `stty raw -echo; fg` → `reset`
   → 至此 su / ssh / vim / top / Ctrl+C 均正常，终端形态写入时间线

4. 选中 Host-L1 → 点击【出网探测】
   → 自动执行 ICMP/DNS/HTTP/TCP 出站测试
   → 结论：可反向 TCP 出网 → 推荐 frp（备选 chisel）

5. 半自动档：面板生成 frps 本机配置 + frpc 靶机命令
   → 选手在虚拟终端粘贴执行 → 面板监听回连成功
   → 拓扑自动画出 L1→本机 的 ProxyLink 边
   → 自动登记统一 Socks 入口（如 127.0.0.1:10801）

6. 一键生成 proxychains 配置 → 选手用 nmap 经代理扫第二层
   → 扫描结果导入（或内嵌扫描器）→ 发现 Host-L2
   → 拿到 L2 的 Shell → 重复第 2–3 步固化终端 → 拓扑上从 L1 挂出 L2 节点

7. 重复 2–6 纵深至 L3（域）→ 全程凭据/Flag/事件自动入库

8. 赛后 →【导出】→ Markdown Writeup 初稿 + HTML 拓扑报告
```

---

## 7. 分期规划

> 需求方期望"大而全一期交付"，以下为建议的内部里程碑切分（不改变一期范围，仅排开发次序），P2 项明确划入二期。

### 一期（MVP→完整版，按里程碑推进）

| 里程碑 | 内容 | 验收标准 |
|---|---|---|
| **MS1 骨架** | 项目/资产/时间线数据模型 + Web 面板框架 + 拓扑图只读渲染 | 能手动登记主机并在拓扑展示 |
| **MS2 Shell 闭环** | M1 全部 P0（一句话马生成/连接/虚拟终端/文件管理/**终端固化 M1-8**） | 通过面板拿马、执行命令、传文件，并成功把伪终端固化为交互 TTY |
| **MS3 代理闭环** | M2-1（frp + Neo-reGeorg 两个 Adapter 先行）+ M2-2 出网探测 + M2-3 半自动档 | 拓扑上自动画出第一条代理链 |
| **MS4 纵深** | 剩余 Adapter（nps/EW/Stowaway/Venom/chisel/ligolo-ng）+ 多级串联 M2-4 + 统一 Socks + proxychains/msf 联动 | 三层链路一键/半键拉起 |
| **MS5 资产完整** | 凭据库 + 复用推荐 + 自动档（M2-3 全自动）+ 健康看板 | 拓扑/凭据/时间线三者联动 |
| **MS6 CTF 化与交付** | M4 全部 + M5 速查库 + M6 导出三格式 + 打包发布 | 打一场真实比赛/靶场全流程验收 |

### 二期（P2）

- 冰蝎/哥斯拉协议栈自研兼容（工作量最大项，单独立项）；
- 提权 exp 智能匹配与自动上传执行（M5-2）；
- 团队协作模式（服务端-多客户端，若未来有需求）；
- 社区插件市场（Adapter/命令库/马模板）。

---

## 8. 风险与缓解

| 风险 | 等级 | 缓解 |
|---|---|---|
| 代理工具版本碎片化，Adapter 适配成本高 | 高 | Adapter 接口最小化（只约定生命周期动作）；每个 Adapter 锁定已验证版本并内置二进制管理（自动下载/校验） |
| 全自动档在异形环境（无落地权限/AV 拦截）失败 | 中 | 默认半自动档；自动档失败自动降级回半自动并给出失败原因 |
| Web 面板形态在断网 CTF 内网环境受限 | 中 | 面板与目标环境的通信全部走 Shell/代理通道，面板本身纯本地，不依赖外网 |
| 合规风险（工具被误用） | 中 | 显著免责声明；README 与启动页双声明；不内置任何针对真实目标的 exploit |
| 一期范围偏大导致延期 | 中 | 严格执行里程碑切分，MS3 完成即具备最小可用价值，后续里程碑可独立交付 |

---

## 9. 竞品对比（差异化论证）

| 能力 | 蚁剑/冰蝎/哥斯拉 | frp/EW/Venom | TscanPlus | **本产品** |
|---|---|---|---|---|
| Webshell 管理 | ✅ 强 | ❌ | 部分 | ✅（一期自研马） |
| 代理隧道 | 冰蝎内置单层 Socks | ✅ 强（单点工具） | ❌ | ✅ **八工具统一编排** |
| 多级串联自动化 | ❌ | 手动串 | ❌ | ✅ **拓扑驱动一键串联** |
| 资产/拓扑/凭据 | ❌ | Venom 有基础拓扑 | 部分 | ✅ **一等公民** |
| CTF 场景（Flag/计时/Writeup） | ❌ | ❌ | ❌ | ✅ **独有** |
| 复盘导出 | ❌ | ❌ | HTML 扫描报告 | ✅ MD/HTML/JSON |

---

## 10. 附录

### 10.1 术语表

- **跳板链（Pivot Chain）**：从攻击端出发，经若干层主机逐跳建立的代理路径；
- **统一 Socks 入口**：面板为本机分配的端口，流量经跳板链转发至目标网段；
- **半自动档**：工具生成命令配置、人工执行；**自动档**：工具经 Shell 自动部署执行；
- **出网探测**：判断靶机可建立的出站连接类型（ICMP/DNS/HTTP/TCP），决定隧道选型。
- **伪终端（dumb shell）**：WebShell 提供的「一次请求一次执行」终端，无控制终端（no TTY）、无 job control、无信号处理，交互式程序不可用，`Ctrl+C` 会中断马本身；
- **终端固化（TTY Upgrade）**：通过 Python PTY / `script` / socat / nc+FIFO 等技法，把伪终端升级为**拥有 PTY 的稳定交互终端**，再以 `stty raw -echo` + `reset` 完成收尾，从而支持 `su`/`ssh`/`vim`/`top`/`Ctrl+C`/方向键/Tab 补全（对应 M1-8）；
- **终端形态**：面板对会话的三态标记 —— 伪终端（dumb）→ 半交互（有回显但无 TTY）→ 已固化（交互 TTY），并同步到 Shell 列表与拓扑节点。

### 10.2 一期技术预研清单（开发前需验证）

1. Python 对 frp/nps 子进程的配置生成与回连状态判定（解析日志 vs 查询 API）；
2. Neo-reGeorg 的会话保活与经它再传文件的可行性；
3. 虚拟终端在 WebSocket 下的回显体验基线；
4. 拓扑图库在 100 节点 + 实时状态刷新下的渲染性能；
5. 自定义马在 PHP/JSP/ASPX 三端的最小可用实现；
6. **终端固化（M1-8）**：PTY 判定依据（`tty` / `stty size` / `isatty`）、各技法在不同精简系统上的成功率，以及 `stty raw -echo` 在 WebSocket 双向通道下的透传可行性（含窗口尺寸 `stty rows/cols` 同步）。
